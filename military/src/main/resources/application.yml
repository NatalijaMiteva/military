spring:
  jpa:
      show-sql: true
      generate-ddl: true
      database: postgresql
  datasource:
    url: jdbc:p6spy:postgresql://localhost:5432/military
    username: natalija
    password: admin
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
#    tomcat:
#          # The indication of whether objects will be validated before being borrowed from the pool. default: false
#          test-on-borrow: true
#          remove-abandoned: true
#          # Timeout in seconds before an abandoned(in use) connection can be removed. default: 60
#          remove-abandoned-timeout: 300
#          # The SQL query that will be used to validate connections from this pool before returning them to the caller.
#          validation-query: SELECT 1
#          validation-interval: 60
#          # The maximum number of connections that should be kept in the pool at all times. default 100
#          max-idle: 20
#
#logging:
#  config: classpath:logback-dev.xml
#  level:
#    org:
#      hibernate:
#        SQL: DEBUG
#        type:
#          descriptor:
#            sql:
#              BasicBinder: TRACE
#
#
##mail:
##  active: false
##  host: smtp.gmail.com
##  port: 587
##  username:
##  password: redminebest99
##  protocol: smtp
##  tls: true
##  auth: true
##  from:
#
#
#
#email:
#  baseUrl: http://localhost:8000/
